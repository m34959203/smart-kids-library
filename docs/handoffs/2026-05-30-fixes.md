# Smart Kids Library — фиксы по QA 2026-05-30 (Claude)

Исправления по `docs/handoffs/2026-05-30-cowork-qa.md` (часть 2 — верификация).
Применено напрямую в репо, `tsc --noEmit` и `next build` зелёные, образ передеплоен.

## Сделано

### P1-1 — заземление ИИ-консультанта на каталог
- `src/app/api/chatbot/route.ts`: для режимов `general` и `search` перед
  `dispatchChat` вызывается `searchBooks(message, 8)` (переиспользован
  существующий FTS из `src/lib/books.ts`: `to_tsvector('russian', title+description)
  @@ plainto_tsquery('russian', …)`, ранжирование `ts_rank`).
  Результаты подмешиваются в system-prompt + жёсткая директива: упоминать
  ТОЛЬКО найденные книги, иначе — «не нашёл, попробуйте /catalog». Пустая
  выдача → явный запрет выдумывать. В ответ добавлено поле `groundedCount`.
  Режим `education` не трогался (школьная программа, не фонд).
- Нового файла-хелпера не делал — `searchBooks` уже был.

### P1-3 — события: детальная страница из общего источника + guard на прошедшее
- Таблица `events` в БД ПУСТАЯ (count=0), витрина была на хардкоде, причём
  деталь (`events/[id]`) показывала ОДИН статичный мок для любого id с вечной
  кнопкой «Записаться». Чтобы не сломать демо пустым календарём — общий
  источник демо-данных:
  - `src/lib/events-data.ts` (NEW): `DEMO_EVENTS` (5 событий с полными
    полями, id 1–5), `isEventPast()`, `getDemoEvent(id)`, `localizeEvent()`.
  - `events/[id]/page.tsx` (REWRITE): берёт событие по `id` из общего
    источника; нет → `notFound()`; `isPast` → вместо кнопки «Записаться»
    показывает «Регистрация на это событие завершена» + бейдж «Прошло/Өткен».
  - `events/page.tsx` (CHANGED): строит список из `DEMO_EVENTS` (те же id,
    что в детали — клик ведёт на корректную страницу).
- **Follow-up:** (а) засидить таблицу `events` и перевести чтение на БД
  (`getMany`/`getOne`, GET `/api/events` уже умеет `?id=`/`?upcoming=true`);
  (б) P2-2 — бейдж «Прошло» в самом календаре `EventCalendar.tsx` (380-строчный
  client-компонент, в этой сессии его тело не редактировал из осторожности —
  отдельная мелкая правка); (в) реальная запись участника = новый
  `POST /api/events/register` с `409` для прошедших.

### P1-4 — TTS: таймаут на клиенте (бэкенд исправен)
- `src/lib/tts-client.ts` (NEW): `fetchTtsAudio()` с `AbortController`
  (таймаут 15 c) + проверкой `Content-Type` (audio vs JSON-ошибка) + пустого
  тела. Возвращает дискриминированный union `{ok}|{reason}`.
- `BookTTS.tsx`, `StoryPlayer.tsx` (CHANGED): переведены на хелпер — теперь
  «Готовлю…» не висит вечно, при таймауте/ошибке показывается «Озвучка
  недоступна». `URL.revokeObjectURL` подчищается во всех ветках.

### P2-3 — `/profile/login` → 404
- `src/app/[locale]/(public)/profile/login/page.tsx` (NEW): редирект на
  `/${locale}/profile?next=…` с сохранением `next`.

## Проверки
- `npx tsc --noEmit` → 0 ошибок.
- `npm run build` → успешно, роут `/[locale]/profile/login` в манифесте.
- i18n: новые строки («Прошло/Өткен», «Регистрация завершена») заданы
  инлайн-тернарником в компонентах — JSON `messages/*.json` НЕ трогал
  (чтобы не сломать парсинг).

## НЕ сделано
- P2-1 (контраст первых обложек) — нужна инспекция БД (SQL в QA-handoff).
- P2-2 бейдж в `EventCalendar` — см. follow-up выше.
- БД-сидинг событий — follow-up.
